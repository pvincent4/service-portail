'use strict';
angular.module( 'portailApp' )
  .run( [ '$templateCache',
    function( $templateCache ) {
      $templateCache.put( 'views/apps.html',
                          '<div class="row damier gris4">  <div data-ng-repeat="case in cases"       class="col-xs-12 col-sm-6 col-md-3 col-lg-3 petite case {{ case.couleur }}"       data-ng-if="current_user.is_logged">    <a class="animate scale-fade-in"       data-ng-if="case.app.active"       data-ui-sref="app-wrapper({ app: case.app.id })"       title="{{ case.app.survol }}">      <span class="compteur-notification orange-brillant"	    data-ng-if="case.app.notifications > 0">{{case.app.notifications}}</span>      <img data-ng-src="{{prefix}}/{{case.app.icone}}"	   data-ng-if="case.app.icone"/>      <span class="app-name">{{ case.app.nom }}</span>    </a>    <div class="configure" data-ng-if="case.app.active && case.app.configure">      <span class="app-name">{{ case.app.nom }}</span>      <button class="btn btn-lg" data-ng-click="case.app.deactivate()">Désactiver</button>    </div>    <button class="btn btn-lg toggle-configure"	    data-ng-if="case.app.active && current_user.profil_actif.admin && case.app.id != \'AIDE\' && case.app.id != \'ADMIN\'"	    data-ng-click="case.app.toggle_configure()">      <span class="glyphicon glyphicon-cog"></span>    </button>  </div></div>' );     } ] );